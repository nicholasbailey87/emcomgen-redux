# broccoli-ml and gradboard are pinned to commit SHAs, not branches. A branch
# ref is not a pin: `neovit` was used for both, and broccoli's was
# fast-forwarded onto main, taking it from 27.1.1 to 30.1.0. That added a Q/K
# RMSNorm and an output RMSNorm to every attention and feedforward block, and
# flipped the `pre_norm`/`post_norm` defaults. The result was the cluster and
# the local venv running different architectures off this same file, and
# checkpoints that cannot be resumed across the boundary (the new `out_norm`
# entries are missing keys under a strict load).
#
# broccoli-ml 30.1.0 (rollback: 27.1.1 = d50d2e8689659947cade11c06000c1bc70bc019b)
broccoli-ml @ git+https://github.com/nicholasbailey87/broccoli.git@33d6f21642f754f841a5adbc27ff331b191a4e90
# gradboard 17.0.0 -- where `neovit` already pointed; main is ahead at 17.1.1
gradboard @ git+https://github.com/nicholasbailey87/gradboard.git@ae464db502c1bfcaee2181b040bfd41acf475359

# torch and torchvision were previously absent from this file entirely. These
# are the versions the cluster runs, which is where results are produced; the
# cluster's `+cu130` local version segment still satisfies these specifiers.
torch==2.9.0
torchvision==0.24.0

click==8.3.0
colorama==0.4.6
einops==0.8.1
h5py==3.15.1
Jinja2==3.1.6
joblib==1.5.2
MarkupSafe==3.0.3
ninja==1.13.0
nltk==3.9.2
numpy==2.3.4
pandas==2.3.3
pillow==12.0.0
POT==0.9.7.post1
psutil==7.1.2
python-dateutil==2.9.0.post0
pytz==2025.2
rapidfuzz==3.14.5
regex==2025.10.23
scikit-learn==1.7.2
scipy==1.16.2
six==1.17.0
strsimpy==0.2.1
threadpoolctl==3.6.0
toml==0.10.2
tqdm==4.67.1
tzdata==2025.2
ujson==5.11.0
wheel==0.45.1
