# lr_sweep_1_cnn arm: ShapeWorld backbones at 2e-5 under gumbel.
#
# See `01_shapeworld_1e-5.toml` for what this experiment measures and why. This
# file differs from that one in `[optimiser.module_lr]`, and in carrying no
# `[data]` block, since ShapeWorld is the default dataset.
#
# The identity control at this rate is `baseline_lr_sweeps` arm 02 for the
# first 30 epochs, and `baseline_lr_sweeps_2` arms 03 and 04, which run this
# rate under identity at silhouette 0.5 and 0.2.
#
# The one ShapeWorld arm with a shape trajectory under identity: chance over
# epochs 1-3 (0.5026), a trough over 12-17 (0.5064), then a step up held to
# the end (0.5312 over 18-30, ending 0.5342). It also led on `test_acc`, with
# a last-five mean of 0.5907 and epochs 22-23 at 0.6145 and 0.6159, the
# highest in the first sweep on either dataset. That makes it the estimator's
# most informative ShapeWorld rate.

name = "lr_sweep_1_cnn"

[sender_language_model]
# This arm ran the straight-through Gumbel-softmax estimator against a
# `DEFAULT.toml` that was still on `"identity"`, which is what the sweep was
# for. It won, and `estimator` was removed on 2026-09-07 -- gumbel is now
# unconditional and the key raises if set. The block is kept as the record of
# what this arm carried; nothing sets it any more.

# The two vision models, and only them, and keyed by the class each of them
# holds rather than by the module group.
#
# **It has to be this table.** `builder.resolve_module_learning_rates` consults
# `[optimiser.implementation_lr].<group>` *after* `[optimiser.module_lr].<group>`
# and lets it win, because the implementation is the more specific claim. Since
# this sweep's own first pass DEFAULT.toml carries `ResNet56 = 2e-5` and
# `ResNet18 = 5e-5` in both vision groups -- written there from these very arms
# -- so a `[optimiser.module_lr] sender_vision` line here is read, and then
# overridden by the default, and every arm in the folder runs at the same rate
# whatever its filename says. The arms were stated that way until 2026-09-11 and
# would have been inert; nothing has been run from them since the default was
# written.
#
# Stating the rate per class also means a re-run measures what the key is
# actually keyed on. Every other key in `[optimiser.module_lr]` stays at
# DEFAULT.toml's 1e-4, and DEFAULT's entry for the *other* dataset's backbone
# (ResNet18) is untouched and unreachable from here -- this arm never builds one.
[optimiser.implementation_lr.sender_vision]
ResNet56 = 2e-5

[optimiser.implementation_lr.receiver_vision]
ResNet56 = 2e-5

[scheduler]
epochs = 20

[experiment]
repeats = 1
