# lr_sweep_2_sender_vit arm: the speaker's ViT backbone at 5e-5 on CUB.
#
# See `01_shapeworld_1e-5.toml` for what this experiment measures and why.
# This file differs from that one in the swept rate.
#
# ---
#
# **This arm: the speaker's ViT backbone at 5e-5 on CUB**, copying ablation rung 04.

name = "lr_sweep_2_sender_vit"

[data]
dataset = "../data/cub"

[sender]
feature_model = "BirdsViT"

[optimiser.implementation_lr.sender_vision]
# The one key this sweep moves. Every other rate is DEFAULT.toml's,
# including the ones earlier sweeps in this set have already settled.
BirdsViT = 5e-5

[scheduler]
epochs = 30

[experiment]
repeats = 1
