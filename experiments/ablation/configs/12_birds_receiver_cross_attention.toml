# Rung 12: rung 10 with `TransformerCrossAttentionComparer` on the receiver.
#
# The same shape as rung 11 -- the module's width is independent of the
# referents it reads, so nothing has to change for CUB. It comes to 5,272,606,
# exactly rung 11's figure: this comparer is now parameter-identical on the two
# datasets. It used to differ by 960, three more positions at `d_model = 320` in
# an absolute position table sized to `message_length`, and pinning absolute
# position embeddings off everywhere took that last length dependence out with
# it. What carries position now is rotary, whose frequencies do not scale with
# the sequence.
#
# `message_length` comes from the birds defaults (10, against ShapeWorld's 7)
# and is validated against `sender_language_model.message_length` in both
# `parse_config.validate_config` and `build_models`, so it is left alone.

name = "ablation"

[data]
dataset = "../data/cub"

[sender]
feature_model = "ViT2"
prototyper = "AttentionPrototyper"
language_model = "SenderTransformerLM"

[sender_language_model]
# Both must equal `sender_feature_model.d_model`: `SenderTransformerLM`
# layer-norms the incoming prototypes at its own `d_model` and separately
# rejects `token_embedding_size != referent_embedding_size`.
token_embedding_size = 320
d_model = 320
heads = 5    # head_dim 64, as everywhere

# 4, where the latent arm takes 5, and the difference is parameters rather than
# taste. A decoder block carries a cross-attention the encoder block does not,
# so it costs about 4*d_model^2 more: 1,376,137 against 965,897 at 320 wide with
# `ff_inner_size = 576`. At 5 layers this speaker would be 7,309,184 parameters,
# or 1.36x the GRU baseline; at 4 it is 5,933,047, or 1.10x. Three would be
# 0.85x, so 4 is the closest this arm gets.
#
# Two of those numbers moved for two different reasons, and only one of them is
# `ff_inner_size`. Raising it 554 -> 576 for tensor-core alignment (see
# DEFAULT.toml) added about 1.4% here, which changes nothing. The ratios are the
# other reason: they read 1.25x, 1.015x and 0.78x when this comment was written,
# against a `SenderGRULM` baseline about 7% larger than the one the code now
# builds. The counts above are measured against the current tree -- 5,371,707 on
# ShapeWorld, 5,380,857 on birds, the two within 0.2% of each other -- so the
# drift is in the baseline, not in this arm.
#
# That reverses one claim this comment used to make. The latent arm at 5 layers
# is now 1.05x, a *tighter* match to the baseline than this arm's 1.10x, where
# it used to be the looser of the two at 0.965x. The ladder still picks 4 here,
# since 3 is further out on the other side, but "closest available" is a weaker
# warrant than "1.015x" was, and the two Transformer arms are no longer matched
# to the same tolerance. Worth re-deriving the ladder against the current
# baseline before the ablation is read as a capacity-matched comparison.
#
# The cost of matching parameters is that the two Transformer arms differ in
# depth as well as in where their layers act. Depth or parameters, not both, and
# the ladder is built on parameters.
layers = 4

# `false`, matching DEFAULT.toml, and the flag does not mean here what it means
# for the GRU baselines -- but it does mean the same *thing*.
#
# The key is shared with `SenderGRULM`, which passes it to
# `nn.GRU(bidirectional=)`; `false` there is a speaker that generates left to
# right and cannot read its own message backwards. On `SenderTransformerLM`
# `false` selects an architecture rather than a mask: a proper autoregressive
# decoder, whose blocks run at message length and cross-attend into the latent
# array, sampling one symbol at a time with each conditioned on the symbols
# before it. `true` selects the other arm -- the Perceiver IO shape, whose
# self-attention runs over the latent array and whose symbols are all emitted in
# one shot. Rungs 13 and 14 run that arm; see their headers.
#
# So `false` is the causal, left-to-right speaker on both classes, which is what
# the word already meant for the GRU. What it no longer does on this class is
# mask the *latent* array left to right, which is what it used to do and which
# bought nothing: the latent array is not a sequence in time, so that mask only
# hid most of the image from the low-index latents.
#
# This is the rung that makes the ladder honest. Against a GRU baseline, the
# `true` arm moves two things at once -- GRU to Transformer, and autoregressive
# to parallel -- so whatever it measures cannot be attributed to architecture.
# This arm moves one.
bidirectional = false

[receiver]
feature_model = "ViT2"
comparer = "TransformerCrossAttentionComparer"

[receiver_comparer]
# Only `layers` is set here. This module's width is free of every other width in
# the run -- it adapts the referents (`referent_adapter`) and the message
# (`message_adapter`) into its own space -- so it could be sized to hit a budget
# exactly, and used to be, at 256 wide with a feedforward inner size of 256.
#
# It takes DEFAULT's 320 / 5 heads / 554 instead, so that every transformer in
# the repo is one width, one head dimension (64) and one feedforward proportion
# (1.73), and depth is the only thing that varies between them. Sizing this one
# to a target meant either an inner size of 256 -- a proportion of 1.0 where
# everything else is 1.73 -- or 443, which would have put it 1.30x over the
# comparer it replaces. Neither is worth an exact match.
#
# 4 layers is then the closest depth: 5,272,606 parameters on both datasets,
# against `BilinearGRUComparer`'s 5,015,553 on the ViT referents of rungs 9 and
# 10, or 1.05x. That is looser than the 1.01x the
# hand-sized version reached, but tighter than the vision arms, which sit at
# 0.91x and 1.11x because a ViT's patch tokeniser scales with image size.
#
# `layers` is the message encoder's depth and nothing else's. It used to be a
# total, split inside the module between a reading stack and a fusion stack, so
# asking for one more block moved two and `layers = 5` bought a 3-layer encoder.
# The module now has one stack -- three bare attention stages surround it, at
# fixed cost -- so the number means what it says. 3 layers would be 0.86x and 5
# would be 1.24x; 4 is the only depth inside the 10% the ladder aims at.
#
# `d_model` is the only width restated here. DEFAULT's 1024 in that section is
# `BilinearGRUComparer`'s hidden size, not this class's; `heads`,
# `ff_inner_size` and the rest are read only by this class and are inherited.
d_model = 320
layers = 4

# `true`, and for the same reason rung 7 sets it on `[sender_language_model]`:
# no transformer in this ladder runs a causal mask. The key is shared with
# `BilinearGRUComparer`, which passes it to `nn.GRU(bidirectional=)` and where
# DEFAULT.toml's `false` still applies; `TransformerCrossAttentionComparer`
# instead turns it into `causal = not bidirectional` on the stack that encodes
# the incoming message (`receiver.py`). Masking that stack left to right models
# a listener that must commit to an interpretation of symbol 1 before seeing
# symbol 5, which is not the listening task -- the message arrives whole, and
# the three attention stages around it are already `causal=False`.
bidirectional = true

[scheduler]
epochs = 30

[experiment]
repeats = 1

[slurm]
time = "24:00:00"
cpus_per_task = 6
gpus_per_task = 1
mem_gb = 24
